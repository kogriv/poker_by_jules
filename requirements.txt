Flask>=2.0
Flask-SocketIO>=5.0
python-socketio>=5.0 # Often a dependency of Flask-SocketIO, good to specify
# eventlet or gevent can be added here if specific async mode is required for production
# e.g., eventlet>=0.30.0
